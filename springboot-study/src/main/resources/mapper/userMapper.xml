<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itmayiedu.member.controller.dao.UserDao">

<select id="findUserByName" parameterType="String" resultType="com.itmayiedu.member.controller.bean.UserBean">
    SELECT * FROM user WHERE userName = #{name}
</select>
</mapper>

